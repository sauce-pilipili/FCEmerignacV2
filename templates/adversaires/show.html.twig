{% extends 'base.html.twig' %}
{% block title %}Adversaires{% endblock %}
    {% block body %}
        <div class="main">
            {{ include('include/_navigation.html.twig') }}
            <div class="content">
                <div class="welcome-container">
                    <h1>Adversaires</h1>

                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ adversaire.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ adversaire.nom }}</td>
                        </tr>
                        <tr>
                            <th>Logo</th>
                            <td>{{ adversaire.logo }}</td>
                        </tr>
                        </tbody>
                    </table>

                    <a href="{{ path('adversaires_index') }}">back to list</a>

                    <a href="{{ path('adversaires_edit', {'id': adversaire.id}) }}">edit</a>

                    {{ include('adversaires/_delete_form.html.twig') }}
                </div>
            </div>
        </div>

    {% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/root.css') }}">
    <link rel="stylesheet" href="{{ asset('css/navigation_back_office.css') }}">
    <link rel="stylesheet" href="{{ asset('css/container_central.css') }}">
{% endblock %}