{% extends 'base.html.twig' %}
{% block title %}Adversaires{% endblock %}
    {% block body %}
        <div class="main">
            {{ include('include/_navigation.html.twig') }}
            <div class="content">
                <div class="welcome-container-title">
                    <h1>Modifier l'adversaire</h1>
                    <a class="bouton-2" href="{{ path('adversaires_index') }}">Quitter <i class="fas fa-times"></i></a>
                </div>
                <div class="welcome-container">
                    {{ include('adversaires/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>

    {% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/formulaire.css') }}">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/imagePreview.js') }}"></script>
    <script src="{{ asset('js/images.js') }}"></script>
{% endblock %}