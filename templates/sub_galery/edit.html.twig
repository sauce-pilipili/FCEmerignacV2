{% extends 'base.html.twig' %}
{% block title %}sous galeries{% endblock %}
    {% block body %}
        <div class="main">
            {{ include('include/_navigation.html.twig') }}
            <div class="content">
                <div class="welcome-container">
                    <h1>Edit SubGalery</h1>

                    {{ include('sub_galery/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('sub_galery_index') }}">back to list</a>

                    {{ include('sub_galery/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    {% endblock %}
